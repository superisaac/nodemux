---

bind: 127.0.0.1:9990
# auth:
#   bearer:
#     token: beartoken
# tls:
#   certfile: localhost.crt
#   keyfile: localhost.key
metrics:
  auth:
    basic:
      username: sss
      password: ppp
    # bearer:
    #   token: sssppp
entrypoints:
  - chain: binance-chain
    network: mainnet
    bind: 0.0.0.0:9999
    # tls:
    #   certfile: localhost.crt
    #   keyfile: localhost.key
